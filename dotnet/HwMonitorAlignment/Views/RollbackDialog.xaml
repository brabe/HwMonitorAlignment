<Window x:Class="HwMonitorAlignment.Views.RollbackDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apply Changes?"
        Width="380" Height="200"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="ToolWindow"
        Topmost="True"
        ShowInTaskbar="True">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Heading -->
        <TextBlock Grid.Row="0"
                   Text="Desktop configuration changed"
                   FontFamily="Segoe UI"
                   FontSize="15"
                   FontWeight="SemiBold"
                   Foreground="White"
                   Margin="0,0,0,8"/>

        <!-- Body text -->
        <TextBlock Grid.Row="1"
                   Text="Do you want to keep these display settings?"
                   FontFamily="Segoe UI"
                   FontSize="12"
                   Foreground="#FFD0D0D0"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <!-- Countdown label -->
        <TextBlock Grid.Row="2"
                   x:Name="TbCountdown"
                   FontFamily="Segoe UI"
                   FontSize="12"
                   Foreground="#FFA0A0A0"
                   VerticalAlignment="Center"/>

        <!-- Progress bar -->
        <ProgressBar Grid.Row="3"
                     x:Name="PbCountdown"
                     Height="6"
                     Minimum="0"
                     Maximum="15"
                     Value="15"
                     Foreground="{StaticResource AccentBrush}"
                     Background="#FF3F3F46"
                     BorderThickness="0"
                     Margin="0,4,0,12"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="BtnRevert"
                    Content="Revert"
                    Style="{StaticResource DarkButtonStyle}"
                    Margin="0,0,8,0"
                    Click="BtnRevert_Click"/>
            <Button x:Name="BtnKeep"
                    Content="Keep Changes"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="BtnKeep_Click"/>
        </StackPanel>
    </Grid>
</Window>
